{
  "name": "voice-capture-macos",
  "version": "1.0.0",
  "private": true,
  "description": "声线采样室：离线录音、复听与 48 kHz WAV 导出",
  "author": "Bronc-X",
  "license": "UNLICENSED",
  "main": "desktop/main.cjs",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.js",
    "test:desktop": "playwright test",
    "dist:mac": "electron-builder --config electron-builder.cjs --mac --arm64 --publish never",
    "pack:local": "electron-builder --config electron-builder.cjs --dir --publish never"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "electron": "44.4.5",
    "electron-builder": "26.15.3"
  }
}
